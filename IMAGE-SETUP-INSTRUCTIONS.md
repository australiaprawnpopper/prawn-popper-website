# 📸 图片设置说明

## 需要准备的图片

您上传了两张照片，需要按以下方式保存：

### 1. 夜市摊位照片（排队顾客的照片）
- **文件名**: `night-market-stall.jpg`
- **位置**: 放在 `images/` 文件夹中
- **用途**: 显示在"品牌故事"区域，展示Prawn Popper在夜市的人气

### 2. 包装虾片照片（展示产品包装的照片）
- **文件名**: `packaged-crackers.jpg`
- **位置**: 放在 `images/` 文件夹中
- **用途**: 显示在"产品特色"区域，展示专业包装

## 文件夹结构

创建如下的文件夹结构：

```
prawn-popper-website/
│
├── index.html                        ← 重命名 prawn-popper-website-final.html 为此
│
└── images/                            ← 创建这个文件夹
    ├── night-market-stall.jpg        ← 夜市摊位照片
    └── packaged-crackers.jpg         ← 包装虾片照片
```

## 快速步骤

1. **创建文件夹**
   - 在您的电脑上创建一个新文件夹，命名为 `prawn-popper-website`
   - 在这个文件夹里创建一个子文件夹，命名为 `images`

2. **重命名HTML文件**
   - 将 `prawn-popper-website-final.html` 重命名为 `index.html`
   - 将它放在 `prawn-popper-website` 文件夹中

3. **添加图片**
   - 将夜市排队的照片重命名为 `night-market-stall.jpg`
   - 将包装虾片的照片重命名为 `packaged-crackers.jpg`
   - 将这两张照片放入 `images` 文件夹中

4. **测试**
   - 双击 `index.html` 在浏览器中打开
   - 滚动页面查看图片是否正确显示
   - 如果图片显示为占位符，检查文件名和路径是否正确

## 图片要求（可选优化）

为了最佳显示效果：
- **推荐尺寸**: 1200-1500像素宽
- **文件格式**: JPG 或 PNG
- **文件大小**: 建议小于 500KB（可使用 TinyPNG.com 压缩）

## 完成后

当文件夹准备好后，您可以：
1. 在本地浏览器中测试网站
2. 按照 `DEPLOYMENT-GUIDE.md` 的指示部署到网上

---

如有问题，请检查：
- ✅ 文件名拼写是否完全一致（包括大小写）
- ✅ 文件是否在正确的文件夹中
- ✅ 图片文件扩展名是否为 `.jpg`
